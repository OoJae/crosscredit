# Secrets — NEVER commit
.env
.env.local
*.pem
*.key

# Dependencies
node_modules/

# Foundry artifacts
contracts/out/
contracts/cache/
broadcast/

# Pinned upstream examples (cloned, not vendored into git)
vendor/

# Worker runtime state
worker/state.json

# Build output
web/dist/
*.tsbuildinfo

# OS / editor
.DS_Store
.idea/
.vscode/

# Scratch output from browser automation and one-off state backups — never artifacts.
.playwright-mcp/
*.bak

# Rendered video output and snapshot scratch — large binaries, regenerable from the composition.
videos/*/renders/
demo/*.mp4
demo/*.m4a
videos/*/snaps/
videos/*/capture/

# ─── Internal working docs — local only, never public ────────────────────────
CLAUDE.md
PROGRESS.md
docs/BUILD_GUIDE.md
docs/HUMAN_ACTIONS.md
docs/STRATEGY.md
docs/SUBMISSION.md
docs/submission-details.md
demo/VOICEOVER.md
videos/**/CLAUDE.md
videos/**/BRIEF.md
videos/**/STORYBOARD.md
cache/
demo/
videos/
docs/deck/deck.html
